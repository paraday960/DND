# 🎉 نسخه ۲.۶ — D&D Bot

## 🐛 رفع باگ‌ها
- **`/join`** حالا درست کار می‌کند (قبلاً اشتباهاً فرم ساخت کاراکتر را باز می‌کرد).
- رفع باگ چرخش نوبت دشمن: هیولاها در یک نوبت چند بار حمله نمی‌کردند.
- تشخیص **کریتیکال** فقط روی ۲۰ طبیعی تاس (نه مقدار نهایی با بونوس).
- رفع باگ **Dodge**: وضعیت دفاع بعد از نوبت بازیکن ریست می‌شود.
- سیستم **مرگ‌سیو (Death Save)** بازنویسی شد:
  - بازیکن زمین‌گیر (hp=0) دیگر از چرخش نوبت حذف نمی‌شود.
  - می‌تواند در نوبت خود `/deathsave` بزند.
  - با سه موفقیت، ۱ HP برمی‌گردد؛ با سه شکست، کاراکتر می‌میرد.
  - بعد از heal در نبرد، وضعیت downed پاک می‌شود.
- همگام‌سازی HP بین `Character` و `combat participant` تقویت شد.
- `setMyCommands` در حالت webhook هم اجرا می‌شود.
- محاسبه HP کاراکترهای سطح‌بالاتر (میانگین hit_die به‌جای فقط یک hit_die).
- فلگ `per_message=False` برای `ConversationHandler` (حذف Warning).
- پوشه پیش‌فرض Termux در `boot.sh` به `~/DND` تغییر یافت.

## ✨ امکانات جدید
- دکمه **🛡️ دفاع (Dodge)** و **💀 نجات از مرگ** به مینی‌گیم وب اضافه شد.
- وضعیت‌های بصری جدید برای بازیکن زمین‌گیر (😵) و مرده (💀) در مینی‌گیم.
- endpoint جدید `/api/combat/deathsave` در وب‌اپ.
- فیلد `race_emoji` و `downed/dead` در API state.
- دستور `/join` پیام واضح‌تری می‌دهد و کد را برمی‌گرداند.

## 🧪 تست
- ۲۴ تست واحد سبز هستند.
- تست‌های اضافی برای جریان کامل نبرد (شروع، حمله، زمین‌گیر شدن، مرگ‌سیو، مرگ) انجام شد.
