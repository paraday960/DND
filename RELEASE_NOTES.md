# 📶 نسخه ۲.۲۹ — مقاوم‌سازی شبکه تلگرام (رفع خطاهای "No route to host" / "Remote end closed")

## مشکل
گزارش‌های لاگ روی اندروید نشون میداد که گاهی long-polling تلگرام به خاطر نوسان اینترنت موبایل یا ISPهای مسدودکننده با خطا مواجه میشه:
- `Remote end closed connection without response` (timeout کوتاه‌تر از تلگرام باعث بستن زودهنگام سوکت میشد)
- `[Errno 113] No route to host` (قطعی لحظه‌ای دیتا/وای‌فای)

بعد از هر خطا، بات ۵ ثانیه منتظر می‌موند، اما وقتی پشت سر هم خطا می‌خورد دوباره و دوباره تلاش می‌کرد و لاگ‌ها را پُر می‌کرد.

## اصلاح
1. **timeout لانگ‌پول تلگرام ۴۰ ثانیه** شد (به‌جای ۲۰). تلگرام در long-poll mode تا ۳۰ ثانیه صبر می‌کنه تا آپدیت داشته باشه؛ با تایم‌اوت ۲۰ ما زودتر از سرور قطع می‌کردیم و `Remote end closed` می‌گرفتیم.
2. **Exponential backoff** برای خطاهای شبکه: با هر شکست متوالی، زمان انتظار از ۲ ثانیه شروع و تا ۲۵ ثانیه افزایش می‌یابد. وقتی اتصال درست شد، شمارنده صفر می‌شود.
3. **لاگ‌هِرز**: اولین خطا، بعداً هر ۶۰ ثانیه یا هر ۵ شکست، یک لاگ نوشته می‌شود تا دیگه هر ثانیه لاگ نریزد.
4. سرور دوباله (Flask + stdlib fallback) از v2.28 سر جایش است.

---
# 🐉 نسخه ۲.۲۸ — تعمیر اساسی مینی‌گیم اندروید + افکت‌های واقعی Boss

## 🚑 مشکل اصلی که کار را خراب می‌کرد
اپ اندروید با فرض اینکه `Flask` همیشه در چاکوپی نصب می‌شود سرور را بالا می‌آورد. اما در اولین اجرا (یا اگر نصب pip به هر دلیلی شکست بخورد) `from webapp import build_app` خطا می‌داد و سرور اصلاً استارت نمی‌شد → مینی‌گیم کامل ۴۰۴ / خطا می‌داد و حتی صفحه‌ی اول هم باز نمی‌شد.

### ✅ راه‌حل دو لایه
1. **مسیرهای متعدد برای پیدا کردن فایل‌های web** (ایندکس/آیکون): پوشه python/web، assets/web، فایل‌های دیتا اپ را به ترتیب چک می‌کند و از هر کدام که فایل موجود بود استفاده می‌کند (دیگه خطای «index not found» نمی‌دهد).
2. **سرور Fallback کامل**: اگر Flask/werkzeug اصلاً در دسترس نباشند، یک `FullApiHandler` بر پایه‌ی `http.server` استاندارد بالا می‌آید که **تمام endpoint‌های مینی‌گیم** را پوشش می‌دهد:
   - `GET /` → صفحه index.html
   - `GET /icon.png` (و هر فایل استاتیک دیگر)
   - `GET /api/meta`, `GET /api/room/state`
   - `POST /api/room/create|join`, `/api/char/create|levelup`
   - `POST /api/scenario`, `/api/story`, `/api/where`, `/api/where/look` (**و alias `/api/look` که تابه‌حال گم بود**)
   - `POST /api/move`, `/api/check`, `/api/rest`, `/api/disarm`, `/api/deathsave`, `/api/inventory`, `/api/roll`
   - `POST /api/combat/start|attack|cast|dodge|deathsave|skip|end`
   - `GET /healthz` برای سلامت سرور
3. **کش بدنه‌ی درخواست** در BaseHTTPRequestHandler (قبلاً یک‌بار `_body()` رید میکرد و در super دیگر چیزی باقی نمی‌ماند).

## 👑 Boss Ability افکت مکانیکی واقعی
قابلیت ویژه باس در نوبت اول (بر اساس متن ability):
- **احضار هم‌دست**: اگر متن کلماتی مثل «احضار/کمک/مینیون/سرباز» داشته باشد، ۲-۳ هم‌دست (goblin/skeleton/kobold/wolf/rat/bandit) به میدان نبرد اضافه و initiative درست می‌شود.
- **فریاد/غرش/انفجار/AoE**: ۱d8 + اصلاح باس آسیب به **همه بازیکنان** با save WIS (DC13) برای نصف‌کردن آسیب + وضعیت «ترسیده».
- **زره/دفاع/درمان**: +2 AC و 8 HP درمان.
- **حالت عمومی**: یک حمله قدرتمند اضافی با +2 به اصابت.

## 🧹 اصلاحات دیگر
- alias `/api/look` در Flask اضافه شد (که فرانت به `/api/where/look` زده و قبلاً کار می‌کرد، اما برای اطمینان هر دو مسیر هستند).
- endpoint عیب‌یابی `GET /api/log` برای دیدن لاگ‌های سرور در هنگام باگ.
- versionCode = 30، versionName = 2.28
- همه ۲۴ تست واحد پاس می‌شوند ✅
- تست اندپوینت‌های Flask **و** fallback با موفقیت پاس شدند.

---

# 🎉 نسخه ۲.۲۳ — راگ‌گیری مینی‌گیم (webapp) و نمایش سینمایی

## 🐛 باگ‌های رفع‌شده در مینی‌گیم
1. **`ABILITY_FA` به‌جای `ABILITIES_FA` در `webapp.py`**: ایمپورت در سطح ماژول `ABILITY_FA` بود اما داخل `sheet_of` به‌اشتباه `ABILITIES_FA` استفاده می‌شد → موجب `NameError` در `/api/char/create`.
2. **ایمپورت در میانه‌ی تابع**: `ABILITIES_FA` داخل `sheet_of` با `from game.rules import ABILITY_FA as ABILITIES_FA` ایمپورت می‌شد؛ تمیز شد و به بالای فایل منتقل شد.
3. **دو تعریف متد `/api/combat/deathsave`** روی هم می‌افتاد و هندلر بیرون-از-نبرد دیده نمی‌شد. هندلر بیرون-نبرد به `/api/deathsave` منتقل شد (در نبرد همان `/api/combat/deathsave` است که جریان `do_advance` را رعایت می‌کند).
4. **HP هیولاها در UI صفر نمایش داده می‌شد**: `combat_of` برای هیولاها `max_hp` را از روی `p.get("max_hp", p.get("hp"))` می‌ساخت که مقدار `hp` لحظه‌ای (گاهی صفر) را به عنوان `max_hp` ثبت می‌کرد. الان `start_combat` در `combat.py` به‌صراحت `max_hp` را از HP اولیه می‌گذارد و کش `_max_hp_cached` به‌عنوان fallback اضافه شد.
5. **آیکون همه هیولاها `👹` بود**: اموجی کلاس/نژاد و هیولا در ساختن participant به درستی از `MONSTERS[k]["emoji"]` و `CLASSES[cls]["emoji"]` گرفته می‌شود (گرگ 🐺، گابلین 👺، اسکلت 💀، اژدها 🐉، جنگجو ⚔️، جادوگر 🧙 و…).
6. **نشان باس**: هیولاهایی که `is_boss=True` بودند با 👑 کنار اسم در لیست نبرد نمایش داده می‌شوند؛ HP باس جدا و بیشتر است.
7. **ساختار سناریوی خام به مرورگر می‌رفت**: secret/NPCها و `twist` (پیچ داستانی) مستقیم به کلاینت ارسال می‌شد. تابع `scenario_view` اضافه شد که یک نسخه‌ی پالوده (بدون twist، بدون secret) به فرانت می‌دهد.
8. **`locations` دیکشنری در فرانت با string.join شکسته می‌شد**: تابع `_locName`/`_npcName`/`_tresText` در JS اضافه شد تا dict-locationها به‌درستی رندر شوند (نام، توصیف، hint).
9. **نوار مکان فعلی**: در کارت سناریو، مکان فعلی با پس‌زمینه طلایی برجسته می‌شود.

## ✨ امکانات جدید مینی‌گیم
- **📍 سیستم حرکت (`/api/move`)**: دکمه‌های «جلو برو»، «عقب برگرد» و ناوبری بین مکان‌ها.
- **👁️ `lookAround` / `/api/where/look`**: نمایش توصیف مکان فعلی، وضعیت نور، خروجی‌ها، مکان‌های بازدیدشده.
- **🔧 `/api/disarm` (خنثی‌سازی تله)**: با چک DEX + پاداش برای rogue/ranger/monk/fighter — شکست تله را فعال می‌کند.
- **🔥 دکمه استراحت کوتاه** در نوار پایین داستان.
- **🔀 چیپس انتخاب‌های داستانی**: شاخه‌های سناریو (branches) به صورت دکمه‌های قابل‌کلیک در کارت سناریو نمایش داده می‌شوند؛ با کلیک متنشان داخل input قرار می‌گیرد.
- **🎲 تاس‌های اضافه**: `d20`, `2d6`, `d8`, `dis` در چیپس‌ها.
- **🐉 کارت سناریو بازنویسی شد**: hook (شروع داستان) به‌صورت ایتالیک، مکان‌ها با hint کمین، NPCها با نقش، باس، تله‌ها با DC تشخیص.
- **emoji در میدان نبرد**: از MONSTERS/CLASSES گرفته می‌شود؛ HP-bar و نام با آیکون درست.
- **افزودن فیلد `emoji` به participant در combat**: برای نمایش بهتر در UI.

## 🪛 اصلاحات چرخه‌ای
- `do_advance` برای نبرد مطمئن می‌شود که وقتی همه دشمنان می‌میرند `end_combat` صدا زده می‌شود.
- `combat.py` به `CLASSES` دسترسی دارد برای گرفتن emoji کاراکتر.

## 🧪 تست
- ۲۴ تست واحد ✅
- ۲۰۰ فاز فازتست ✅
- ۱۰۰۰ فاز الترا فازتست ✅
- تست یکپارچه وب‌اپ (healthz، meta، ساخت اتاق/کاراکتر/سناریو/نبرد) ✅

---

# 🎉 نسخه ۲.۲۲ — D&D Bot (سناریوهای سینمایی عمیق با حلقه لوپ‌انجینیرینگ)


## 🎭 سناریوهای سینمایی جدید (بزرگ‌ترین تغییر!)
- **اسکلت جدید سناریو** (`SCENARIO_PROMPT`): هر سناریو الان شامل این‌هاست:
  - `title`, `hook`, `goal` — عنوان، صحنه سینمایی شروع، هدف احساسی
  - `locations[]` — مکان‌ها به صورت دیکشنری با `name`, `description`, `encounter_hint`
  - `npcs[]` — شخصیت‌ها با `name`, `role`, `secret` (راز/انگیزه پنهان)
  - `encounters[]` — دشمنان با `location` نسبت به مکان‌ها
  - `treasure[]` — لیست گنج‌ها با آیتم/تعداد/توصیف
  - 🪤 **`traps[]`** — تله‌ها با `detect_dc`, `disarm_dc`, `damage`, `effect`, `trigger`, `location`
  - 🔀 **`branches[]`** — ۲ تا ۳ انتخاب با عواقب متفاوت
  - 🌀 **`twist`** — پیچ داستانی غیرمنتظره
  - 👑 **`boss`** — باس‌فایت نهایی با قابلیت ویژه (`ability`)
- **`_normalize_scenario`**: هر خروجی AI قبل از استفاده، پارس، تمیز و بالانس می‌شود:
  - حداکثر ۶ هیولا برای سطح ۱، ۷ برای سطح ۳، ۸ برای سطح ۵
  - ممانعت از troll/dragon/harpy برای سطح ≤۲
  - فیلتر کردن دشمنان نامعتبر
  - boss فقط در صورت ظرفیت خالی و با ممانعت از انواع خیلی قوی برای سطح پایین
  - تضمین وجود حداقل یک مکان/NPC/قلاب
- **`_fallback_scenario` (حالت آفلاین) بازنویسی کامل شد**:
  - سه سطح سختی مجزا (۱-۲ / ۳-۴ / ۵+)
  - شامل مکان‌های غنی، NPC با secret، تله، شاخه، پیچ و باس
  - در صورت خطای AI (timeout/نامعتبر) به این سناریوی غنی فال‌بک می‌کند
- **کپ سختی**: فیکس باگ `max(1, 0)` که باعث می‌شد یک هیولا اضافه‌تر از سقف ظاهر شود.

## 🪤 سیستم تله (Trap System)
- **`_check_trap_trigger` در world.py**:
  - تشخیص تله در مکان فعلی هنگام اکشن‌های خطرناک (باز کردن، ورود، فشار دادن، قدم زدن)
  - پرتاب WIS (Perception) با پاداش proficiency برای ranger/rogue/monk
  - موفق: هشدار با DC خنثی‌سازی
  - شکست: آسیب تصادفی + اعمال به HP + لاگ
- **`try_disarm_trap`**: خنثی‌سازی با DEX (Sleight of Hand) — شکست تله را فعال می‌کند
- تله هنگام **move** بین مکان‌ها هم بررسی می‌شود (ورود به مکان جدید)
- دستور `/check sleight <dc>` حالا به خنثی‌سازی تله منجر می‌شود
- `/check perception` موفق هشدار تله هم می‌دهد
- نام‌های فارسی `/check ظرافت|خنثی|تله|ادراک|بینایی|پنهان|...` هم پشتیبانی می‌شوند

## 🗺️ بهبود نقشه و جابجایی
- **`map.py` به‌روزرسانی شد** تا با `locations` دیکشنری کار کند
- **`move_to`** حالا:
  - توصیف مکان جدید را در اولین بازدید نشان می‌دهد
  - تله‌های مکان جدید را چک می‌کند
  - حضور دشمنان در مکان را اطلاع می‌دهد (با علامت 👑 برای باس)
  - hint مکان را نمایش می‌دهد
- سازگاری کامل با world ذخیره شده (اگر location قبلاً str بود هم کار می‌کند)

## 👑 سیستم Boss در نبرد
- باس‌ها به‌صورت پیش‌فرض **دو حمله** در هر نوبت دارند (multiattack)
- در اولین نوبت، **قابلیت ویژه** باس (`ability`) اعلام می‌شود
- نمایش 👑 در لیست دشمنان و scenario_text
- XP مضرب برای باس
- HP و AC کمی بالاتر از دشمن معمولی هم‌نوع

## 🔧 بهبودهای چرخه (Loop) و کیفیت زندگی
- **`scenario_text()` بازنویسی**: نمایش زیبای مکان‌ها، NPCها با نقش، تله‌ها، شاخه‌ها، پیچ داستانی، گنج ساختاریافته
- **`_context` برای DM (AI)**: شامل عنوان/هدف، مکان‌ها، دشمنان با تعداد، پیچ داستانی (مخفی از بازیکن)، نور، مکان فعلی، وضعیت‌ها
- **SYSTEM_PROMPT**: افزودن قانون «اکشن‌های ساده مثل روشن کردن مشعل را در روایت تایید کن»
- نرمال‌سازی world بعد از ساخت سناریو چه در حالت AI و چه در fallback
- **`_lead_character`**: در غیاب بازیکن مشخص (مثلاً هشدار عمومی تله) از اولین کاراکتر زنده استفاده می‌شود

## 🐛 رفع باگ‌ها
- باگ بزرگ اندنتیشن در narrator.py که چند متد را از کلاس خارج کرده بود (از جلسه قبل همچنان پابرجا بود)
- باگ کپ هیولاها (شرح بالا)
- تایپو (`)` به‌جای `"`) در پیام تله world.py
- کد تکراری صندوقچه در world.py حذف شد
- Boss برای سطح ۱-۲ به‌جای غول‌های خیلی قوی، از یک نسخه قوی‌تر دشمن ضعیف استفاده می‌کند
- fallback سناریو هم از `_normalize_scenario` رد می‌شود تا همیشه ظرفیت سختی رعایت شود
- ناسازگاری map با dict-locations رفع شد
- خطای None بودن ch در چک تله هندل شد

## 🧪 تست (لوپ انجینیرینگ)
- `run_tests.py`: ۲۴ تست واحد ✅
- `fuzz_test.py`: ۲۰۰ ترکیب تصادفی حزب/سناریو ✅
- `ultra_fuzz.py`: **۱۰۰۰** ترکیب کامبت + ۳۳ جمله NLP + ۱۰۰ بار save/load ✅
- `scenario_test.py`: ۱۲ سناریوی متنوع با درخواست به Mistral و اعتبارسنجی ساختار JSON ✅
- تست زنده با Mistral: سناریوی «دخمه اسکلت‌ها» با ۶ هیولا (رعایت کپ سطح ۱)، تله، شاخه و پیچ ✅

---

# 🎉 نسخه ۲.۲۱ — D&D Bot (تعمیق لوپ‌-انجینیرینگ)

## 🐛 رفع باگ‌ها
- **پارسر NLP فارسی**: باگ `_detect_spell` که نامش اشتباه بود و هر بار می‌کرشید → به `_extract_spell` اصلاح شد.
- **استخراج هدف در پارسر**: جلوگیری از انتخاب کلمات کلیدی (آتیش، شفا، طلسم، ضمایر) به عنوان نام هیولا.
- **Dodge برای هیولاها**: هیولاها در حمله، وضعیت dodge بازیکن را در نظر می‌گیرند و با ضعف (disadvantage) حمله می‌زنند.
- **Fumble/Crit برای هیولاها**: رول ۱ برای هیولا به‌صراحت «لغزش دست» اعلام می‌شود.
- **مهندسی مجدد auto_act هیولاها**: با تابع کمکی `_resolve_one_attack`؛ هر حمله داژ/فامبل/داون‌شدن را درست هندل می‌کند.
- **همگام‌سازی HP در پایان نبرد**: HP واقعی کاراکترها بعد از نبرد به‌روز می‌شود.
- **بازنشانی death_saves و downed** بعد از شفا یا پیروزی/شکست.
- **رفع باگ محاسبه AC در equip_armor**: مقدار DEX صحیح برای زره متوسط (max +2)، سنگین (۰)، سبک/بی‌زره/ردا (full DEX).
- **رفع import اشتباه**: از `ability_mod` به صورت محلی ایمپورت می‌شد → ایمپورت سراسری استفاده می‌کند.
- **use_item** از great_potion، پادزهر، مشعل هم پشتیبانی می‌کند (قبلاً فقط potion بود).
- **تست run_tests.py**: به‌روزرسانی برای سازگاری با پیام‌های جدید.

## ✨ امکانات عمیق جدید
- **🔥 Multiattack برای هیولاها**: ترول ۲ حمله، اژدهای جوان ۳ حمله، هارپی و عنکبوت ۲ حمله، بقیه ۱ حمله.
- **⚔️ حمله اضافه (Extra Attack) برای بازیکنان**:
  - سطح ۵+: جنگجو/بربر/پالادین/رنجر/مانک ۲ حمله در هر نوبت
  - سطح ۱۱+: جنگجو ۳ حمله
  - سطح ۲۰+: جنگجو ۴ حمله
- **🗡️ Sneak Attack برای Rogue**: وقتی یالِ دیگری کنار دشمن است (۱d6 در سطح ۱–۲، تا ۱۰d6 در سطح ۲۰)، ضربه غافلگیرانه می‌زند.
- **💀 بهبود death_save**: پایدار شدن (۳ موفقیت) بازیکن را به درستی با ۱ HP به هوش می‌آورد؛ سه شکست مردن قطعی.
- **🎁 گنج و لوت بعد از هر پیروزی**: سکه، معجون، مشعل، طناب به صورت تصادفی بین اعضای گروه تقسیم می‌شود.
- **🛠️ بهبود atk_bonus پیش‌فرض هیولاها** بر اساس CR (۱–۷ به جای +۲ ثابت).
- **پارسر NLP**: پشتیبانی از «معجون بزرگ»، «تاس d20»، چندین الگوی جدید فارسی، تطابق فازی بهتر اهداف.
- **سرعت**: همه‌چیز روی ۱۰۰۰ فازتست + ۱۰۰ تست پرزیستنس + ۳۳ تست NLP پاس می‌شود.
